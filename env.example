# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key

# Twilio Configuration (use EU region IE1 for GDPR compliance)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_NUMBER_E164=+49XXXXXXXXXX

# Security
ADMIN_TOKEN=your-secure-admin-token

# Encryption key for transcripts at rest (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=your-fernet-key

# Base URL for webhooks (must be publicly accessible HTTPS for Twilio)
BASE_URL=https://your-domain.eu

# Frontend API URL
VITE_API_BASE_URL=http://localhost:8000

